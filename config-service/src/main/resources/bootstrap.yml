server:
  port: 8888
spring:
  profiles:
    active: native
  security:
    user:
      password: C0nfig$ervice123
      name: config-client
  cloud:
    config:
      server:
        native:
          search-locations: classpath:config/
